<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.manjil.e_prabhu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@drawable/eprabhu"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".Activity.MobileTopActivity" />
        <activity android:name=".Activity.Transfer" />
        <activity android:name=".Activity.Newlayout_designactivity" />
        <activity android:name=".Activity.Favourite" />
        <activity android:name=".Activity.NewNavigation_appbar"></activity>
    </application>

</manifest>